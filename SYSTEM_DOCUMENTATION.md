# ระบบรับสมัครงานออนไลน์ (Online Job Application System)
## เทศบาลเมืองอุทัยธานี

เอกสารฉบับนี้อธิบายรายละเอียดเกี่ยวกับโครงสร้าง หน้าที่การทำงาน และเทคโนโลยีที่ใช้ในระบบรับสมัครงานออนไลน์ของเทศบาลเมืองอุทัยธานี

---

## 🚀 เทคโนโลยีที่ใช้ (Tech Stack)

ระบบพัฒนาด้วยเทคโนโลยีสมัยใหม่เพื่อให้มีความรวดเร็วและรองรับการใช้งานที่หลากหลาย:
- **Frontend Framework:** [React 19](https://react.dev/) + [Vite](https://vitejs.dev/)
- **Styling:** [Tailwind CSS 4](https://tailwindcss.com/)
- **Routing:** [React Router 7](https://reactrouter.com/)
- **Backend & Database:** [Supabase](https://supabase.com/) (PostgreSQL + Auth + Storage)
- **3D Graphics:** [Three.js](https://threejs.org/) + [@react-three/fiber](https://docs.pmnd.rs/react-three-fiber) + [@react-three/drei](https://github.com/pmndrs/drei) (สำหรับกราฟิกหอนาฬิกา 3 มิติ)
- **PDF Generation:** [jsPDF](https://github.com/parallax/jsPDF) + [html2canvas](https://html2canvas.hertzen.com/) (สำหรับออกบัตรประจำตัวสอบ)
- **Excel Export:** [XLSX (SheetJS)](https://sheetjs.com/) (สำหรับส่งออกข้อมูลผู้สมัคร)
- **Notifications:** [Line Notify API](https://notify-bot.line.me/doc/th/) (สำหรับแจ้งเตือนเมื่อมีการสมัครใหม่)

---

## 🛠️ โครงสร้างไฟล์ที่สำคัญ (Project Structure)

```text
src/
├── components/          # ส่วนประกอบ UI ที่ใช้ซ้ำได้
│   ├── wizard/          # ขั้นตอนในหน้าสมัครงาน (Multi-step form)
│   ├── ClockTowerScene.jsx # กราฟิกหอนาฬิกา 3 มิติหน้าแรก
│   └── ExamCardTemplate.jsx # แม่แบบบัตรประจำตัวสอบ
├── lib/
│   └── supabaseClient.js # การเชื่อมต่อกับฐานข้อมูล Supabase
├── pages/               # หน้าหลักของระบบ
│   ├── LandingPage.jsx  # หน้าแรกของระบบ
│   ├── ApplicationWizard.jsx # ระบบสมัครงานออนไลน์
│   ├── ConsentPage.jsx  # หน้ากดยอมรับ PDPA
│   ├── CheckStatus.jsx  # ระบบตรวจสอบสถานะและพิมบัตรประจำตัว
│   ├── AdminLogin.jsx   # หน้าเข้าระบบผู้ดูแล
│   └── AdminDashboard.jsx # ระบบจัดการข้อมูลสำหรับเจ้าหน้าที่
└── utils/               # ฟังก์ชันช่วยเหลือต่างๆ
    ├── exportExcel.js   # ระบบส่งออกไฟล์ Excel
    ├── generateExamCard.js # ระบบสร้างบัตรประจำตัวสอบ
    └── lineNotify.js    # ระบบแจ้งเตือนผ่าน Line
```

---

## ✨ ฟีเจอร์หลักของระบบ (Main Features)

### 1. ระบบผู้สมัคร (Candidate Portal)
- **Landing Page:** แสดงกราฟิกหอนาฬิกา 3 มิติที่เป็นเอกลักษณ์ของเมืองอุทัยธานี
- **Online Application (Wizard):** ระบบกรอกข้อมูลสมัครงานแบบทีละขั้นตอน แบ่งเป็น:
    - ข้อมูลส่วนตัวและรูปถ่าย
    - ประวัติการศึกษาและการทำงาน
    - การอัปโหลดเอกสารหลักฐาน
- **Status Tracking:** ผู้สมัครสามารถตรวจสอบสถานะการสมัครของตนเองได้โดยใช้เลขบัตรประชาชน
- **Exam Card Generation:** เมื่อเจ้าหน้าที่อนุมัติ ผู้สมัครสามารถพิมพ์บัตรประจำตัวสอบที่มี QR Code ได้ทันที

### 2. ระบบผู้ดูแล (Admin Dashboard)
- **Candidate Management:** ดูรายชื่อผู้สมัครทั้งหมด ค้นหา และกรองข้อมูลตามตำแหน่ง
- **Status Update:** เจ้าหน้าที่สามารถตรวจสอบเอกสารและเปลี่ยนสถานะ (รอตรวจสอบ, อนุมัติ, ไม่นุมัติ)
- **Excel Export:** ส่งออกรายชื่อผู้สมัครเป็นไฟล์ Excel เพื่อนำไปใช้งานต่อ
- **Line Notification:** แจ้งเตือนไปยังกลุ่ม Line ของเจ้าหน้าที่ทันทีที่มีคำขอสมัครใหม่เข้ามา

---

## 🔒 การตั้งค่าระบบ (Configuration)

ระบบมีการเชื่อมต่อกับ Supabase โดยใช้ค่าที่ตั้งไว้ใน `src/lib/supabaseClient.js` ซึ่งรวมถึง:
- `supabaseUrl`: URL ของโครงการ Supabase
- `supabaseKey`: API Key สำหรับการเข้าถึงข้อมูล (Publishable Key)

---

## 🚀 วิธีการรันระบบ (Local Development)

1. ติดตั้ง dependencies: `npm install`
2. รันระบบ: `npm run dev`
3. เข้าใช้งานผ่าน: `http://localhost:5173`
